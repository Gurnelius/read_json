# Read JSON data stored in a file

Read a JSON file using JavaScript and output the content into an HTML table.
The Table would get updated automatically (upon page refresh) when the data in the underlying JSON file changes.
